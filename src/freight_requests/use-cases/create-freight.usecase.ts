/* eslint-disable @typescript-eslint/no-unsafe-return */
/* eslint-disable @typescript-eslint/no-unsafe-assignment */
/*import { Inject, Injectable } from '@nestjs/common';
import { CreateFreightRequestDto } from '../dto/create-freight_request.dto';
import {
  FREIGHT_REQUEST_REPOSITORY,
  FreightRequestRepositoryInterface,
} from '../repositories/freight_request.repository.interface';

@Injectable()
export class CreateFreightRequestUseCase {
  constructor(
    @Inject(FREIGHT_REQUEST_REPOSITORY)
    private freightsRepository: FreightRequestRepositoryInterface,
  ) {}
  /**
   * Executes the use case to create a new freight.
   * @param dto - The data transfer object containing freight details.
   * @param clientId - The ID of the client creating the freight.
   * @returns The created freight.
   */
  /*async execute(dto: CreateFreightRequestDto) {
    const freight = await this.freightsRepository.create(dto);
    return freight;
  }
}*/
